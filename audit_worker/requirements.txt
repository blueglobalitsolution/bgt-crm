# BGT CRM Website Audit Worker - Python dependencies
# httpx, lxml, beautifulsoup4 are used for the fast crawler path.
# playwright (with chromium installed) is optional for JS-heavy sites.
httpx>=0.27
lxml>=5.0
beautifulsoup4>=4.12
playwright>=1.44
